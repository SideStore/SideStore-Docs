---
title: Установка через Linux
description: Инструкция по установке SideStore через Linux
sidebar_position: 5
---

Для установки SideStore (или любого другого IPA файла) на Linux, нужно установить Docker. Перед тем как начать, установите StosVPN и остальное из [подготовки](prerequisites)!

1. Запустите altcon через Docker
```bash
docker run --rm -it -e ALTSERVER_ANISETTE_SERVER="https://ani.sidestore.io/" -v ${PWD}/:/mnt/ -v /var/run:/var/run ghcr.io/sidestore/altcon
```

Также возможен запуск altcon через Podman
```bash
podman run --privileged --rm -it -e ALTSERVER_ANISETTE_SERVER="https://ani.sidestore.io/" -v ${PWD}/:/mnt/ -v /var/run:/var/run ghcr.io/sidestore/altcon
```

Altcon начнёт сопряжение с Вашим устройством, подключите устройство, если ещё этого не сделали. Подтвердите запрос на доверие Вашему ПК.

Когда завершите, выполните действия из [инструкции после установки](post-install).
